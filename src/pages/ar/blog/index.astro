---
import BaseLayout from '../../../layouts/BaseLayout.astro';
---
<BaseLayout title="المدوّنة" description="ملاحظات بحثية ومقالات لاسماعيل الجامعي.">
  <div class="page-container">
    <header class="page-header">
      <h1 class="page-title">المدوّنة</h1>
      <div class="page-rule" aria-hidden="true"></div>
    </header>
    <div class="notice">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
      <p>ملاحظات بحثية ومقالات قادمة قريباً.</p>
    </div>
  </div>
</BaseLayout>
<style>
  .page-container { max-width: 720px; margin: 0 auto; padding: 4rem 1.5rem 5rem; }
  .page-header { margin-bottom: 2.5rem; }
  .page-title { font-family: var(--font-arabic); font-size: clamp(2rem, 4vw, 2.75rem); font-weight: 700; color: var(--fg); margin-bottom: 0.75rem; }
  .page-rule { height: 2px; background: linear-gradient(to right, var(--color-olive), transparent); border-radius: 1px; }
  .notice { display: flex; gap: 1rem; align-items: center; padding: 1.25rem 1.5rem; border: 1px solid var(--border); border-radius: 0.375rem; background: var(--bg-panel); color: var(--fg-muted); font-family: var(--font-arabic); font-size: 1rem; }
  .notice svg { flex-shrink: 0; color: var(--accent); }
  .notice p { margin: 0; }
</style>
